default_service: app
user: www-data
dependencies:
  apt:
    - gettext-base
    - moreutils
purge:
  apt:
    - gettext-base
    - moreutils
build_args:
  PHP_VERSION: 7.2
project:
  context: development
modules:
  - single-instance